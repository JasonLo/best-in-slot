# mkdocs-example

Minimal mkdocs-material site.

```sh
uv sync
uv run mkdocs serve
```

!!! tip "Material features"
    The theme ships with dark mode, search, tabs, and code-copy buttons by default.
