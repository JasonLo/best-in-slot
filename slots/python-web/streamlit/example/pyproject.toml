[project]
name = "streamlit-example"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["streamlit>=1.40", "pandas>=2.2"]

[build-system]
requires = ["uv_build>=0.10"]
build-backend = "uv_build"

[tool.uv.build-backend]
module-root = ""
