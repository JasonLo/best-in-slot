[project]
name = "pixi-example"
description = "Minimal pixi project (CPU-only so it boots on any machine)."
channels = ["conda-forge"]
platforms = ["linux-64", "osx-arm64"]
channel-priority = "strict"

[dependencies]
python = "3.12.*"
numpy = ">=2.0"

[tasks]
hello = "python -c \"import numpy as np; print('numpy', np.__version__)\""
