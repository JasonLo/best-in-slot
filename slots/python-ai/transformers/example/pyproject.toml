[project]
name = "transformers-example"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = ["transformers>=4.46", "torch>=2.5"]

[dependency-groups]
dev = ["pytest>=8"]

[build-system]
requires = ["uv_build>=0.10"]
build-backend = "uv_build"

[tool.uv.build-backend]
module-root = ""
