[project]
name = "hf-datasets-example"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["datasets>=3.0", "pyarrow>=18"]

[dependency-groups]
dev = ["pytest>=8"]

[build-system]
requires = ["uv_build>=0.10"]
build-backend = "uv_build"

[tool.uv.build-backend]
module-root = ""
