# astro example

Minimal Astro 6 site with one content collection (`posts`).

```sh
npm install
npm run dev        # http://localhost:4321
npm run build      # static output → ./dist
npm run preview
```
