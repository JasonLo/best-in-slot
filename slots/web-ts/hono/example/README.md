# hono example

```sh
bun install
bun test
bun --watch run src/index.ts        # http://localhost:3000
# in another shell:
curl http://localhost:3000/healthz
curl http://localhost:3000/hello/jason
```
