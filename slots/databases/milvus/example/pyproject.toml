[project]
name = "milvus-example"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["pymilvus[milvus-lite]>=2.4", "numpy>=2.0"]

[dependency-groups]
dev = ["pytest>=8"]

[build-system]
requires = ["uv_build>=0.10"]
build-backend = "uv_build"

[tool.uv.build-backend]
module-root = ""
